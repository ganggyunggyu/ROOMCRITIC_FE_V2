export type User = {
  _id: string;
  email: string;
  displayName: string;
  phoneNumber: string;
  role: string;
};
export type Review = {
  review: string;
};
export type Content = {
  title: string;
};
